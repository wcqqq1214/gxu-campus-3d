# Match the geometry and terrain processing used by the published baseline.
# Also usable with Python 3.12; install with -r requirements.txt -c this file.
Pillow==11.3.0
shapely==2.0.7
mapbox-earcut==2.1.0
numpy==2.0.2
