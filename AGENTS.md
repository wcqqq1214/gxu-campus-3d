# 开发分支规则

用户于 2026-09-15 指定：开发、commit 和 push 均在 `dev` 分支进行。

- 本仓库的主分支实际为 `main`；用户所说的 `master` 指主分支。两种名称均按主分支保护要求处理。
- 修改、提交或推送前确认当前分支为 `dev`。保留已有未提交改动，不因切换分支而丢弃它们。
- 开发提交仅推送到 `origin/dev`，不要直接提交或推送到 `main` / `master`。
- 未取得用户明确允许，不将 `dev` 合并到 `main` / `master`，也不通过快进、变基、挑拣提交或强制推送绕过该限制。
- 普通的“继续开发”“提交”“推送”请求不构成合并或更新主分支的授权。
